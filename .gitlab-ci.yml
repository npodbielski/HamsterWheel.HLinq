image: mcr.microsoft.com/dotnet/sdk:9.0
services:
  - name: postgres
    variables:
      POSTGRES_DB: Demo
      POSTGRES_PASSWORD: outflank-outage-undoing

variables:
  OBJECTS_DIRECTORY: 'obj'
  NUGET_PACKAGES_DIRECTORY: '.nuget'
  SOURCE_CODE_PATH: ''

cache:
  key: "$CI_JOB_STAGE-$CI_COMMIT_REF_SLUG"
  paths:
    - '$SOURCE_CODE_PATH$OBJECTS_DIRECTORY/project.assets.json'
    - '$SOURCE_CODE_PATH$OBJECTS_DIRECTORY/*.csproj.nuget.*'
    - '$NUGET_PACKAGES_DIRECTORY'
  policy: pull-push

before_script:
  - 'dotnet restore HLinq.slnx --packages $NUGET_PACKAGES_DIRECTORY'

build:
  stage: build
  script:
    - 'dotnet build HLinq.slnx --no-restore'

tests:
  stage: test
  script:
    - 'dotnet test HLinq.slnx --logger:"junit;LogFileName=$SOURCE_CODE_PATH/rspec.xml" -no-build --no-restore'
  artifacts:
    when: always
    paths:
      - rspec.xml
    reports:
      junit:
        - rspec.xml

package-alpha:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "alpha"
  script:
    - dotnet pack HLinq.slnx -c Release --no-build --no-restore --version-suffix $CI_JOB_ID
    # Configure GitLab package registry as a NuGet source
    - dotnet nuget add source "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/nuget/index.json" --name gitlab --username gitlab-ci-token --password $CI_JOB_TOKEN --store-password-in-clear-text
    # Push the package to the project's package registry
    - dotnet nuget push "src/**/bin/Release/*.nupkg" --source gitlab
  environment: production

package:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
  script:
    - dotnet pack HLinq.slnx -c Release --no-build --no-restore
    # Configure GitLab package registry as a NuGet source
    - dotnet nuget add source "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/nuget/index.json" --name gitlab --username gitlab-ci-token --password $CI_JOB_TOKEN --store-password-in-clear-text
    # Push the package to the project's package registry
    - dotnet nuget push "src/**/bin/Release/*.nupkg" --source gitlab
  environment: production